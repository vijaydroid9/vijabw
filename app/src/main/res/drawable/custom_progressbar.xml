<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#242f4c"
                android:centerColor="#242f4c"
                android:centerY="1.0"
                android:endColor="#242f4c"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#60C496"
                    android:centerColor="#60C496"
                    android:centerY="1.0"
                    android:endColor="#60C496"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>